import React from 'react'
export const metadata = {
    title: "Envol Technology - Contact",
    description: "Contact Page",
};

export default function layout({ children }) {
    return (
        <>
            {children}
        </>
    )
}
