import React from 'react'

export const metadata = {
    title: "Envol Technology - Services",
    description: "Services Page",
};

export default function layout({ children }) {
    return (
        <>
            {children}
        </>
    )
}
