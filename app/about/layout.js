import React from 'react'
export const metadata = {
    title: "Envol Technology - A propos",
    description: "About Page",
};

export default function layout({ children }) {
    return (
        <>
            {children}
        </>
    )
}
