import React from 'react'

export const metadata = {
    title: "Envol Technology - Clients",
    description: "Clients Page",
};

export default function layout({ children }) {
    return (
        <>
            {children}
        </>
    )
}
